<Page x:Class="Keesh.Interface.User.NavigationPage.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Keesh.Interface.User.NavigationPage"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Keesh" Style="{StaticResource DefaultPageStyle}">
    <Page.Resources>
        <Style x:Key="FlowDocumentStyle" TargetType="FlowDocument">
            <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
            <Setter Property="FontSize" Value="{StaticResource DefaultFontSize}"/>
        </Style>
    </Page.Resources>
    <Grid>
        <FlowDocumentScrollViewer>
            <FlowDocument Style="{StaticResource FlowDocumentStyle}">
                <Paragraph>
                    <TextBlock>Welcome</TextBlock>
                </Paragraph>
                <Paragraph>
                    <Run>Current Objectives:</Run>                    
                </Paragraph>
                <List MarkerStyle="Disc" MarkerOffset="6" Padding="12,0,0,0">
                    <ListItem>
                        <Paragraph>Alpha Vantage Key Handling</Paragraph>
                        <Paragraph>Add a way for users to enter the key. And add a way to cache the key. The first attempt will use a environment variable to store the key.</Paragraph>
                    </ListItem>
                </List>
            </FlowDocument>
        </FlowDocumentScrollViewer>
    </Grid>
</Page>
